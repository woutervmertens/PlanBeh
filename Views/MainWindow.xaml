<Window x:Class="PlanBeh.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:local="clr-namespace:PlanBeh"
        xmlns:views ="clr-namespace:PlanBeh.Views"
        xmlns:vm="clr-namespace:PlanBeh.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="514.989" Width="677.975">
    <Window.Resources>
        <vm:NodeViewModel x:Key="NodeViewModel"/>
    </Window.Resources>
    <Grid>
        <Grid Height="20.709" VerticalAlignment="Top" Margin="0,0,130,0">
            <ToolBar HorizontalAlignment="Left" Height="20.709" VerticalAlignment="Top" Width="519.975" Margin="10,0,0,0"/>
        </Grid>
        <Grid Margin="0,20.709,130,0">
            <ItemsControl ItemsSource="{Binding Path=WorkSpaceCollection}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="{Binding ActualWidth, ElementName=ConsoleWrapper}" Height="{Binding ActualHeight, ElementName=MainViewContainer}" Background="#FF3E3E3E" IsHitTestVisible="True">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDown">
                        <i:InvokeCommandAction Command="{Binding PlaceNodeCommand}" CommandParameter="{Binding ElementName=WorkSpace}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseUp">
                        <i:InvokeCommandAction Command="{Binding StopConnectingCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding Position.X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Position.Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.Resources>
                    <DataTemplate DataType="{x:Type vm:NodeViewModel}">
                        <views:NodeView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:ConnectionViewModel}">
                        <views:ConnectionView/>
                    </DataTemplate>
                </ItemsControl.Resources>
            </ItemsControl>
        </Grid>
        <Grid HorizontalAlignment="Right" Height="319" Margin="385.927,0,0,0" VerticalAlignment="Top" Width="130"/>

    </Grid>
</Window>
